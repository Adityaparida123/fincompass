# Legacy PostgreSQL migration tooling — NOT required at runtime.
# Used only by:  alembic/, scripts/migrate_pg_to_mongo.py,
#                scripts/bootstrap_sqlite_dev.py
# Install with:  pip install -r requirements.txt -r requirements-migration.txt

sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
aiosqlite==0.20.0
alembic==1.14.0
